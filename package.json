{
  "name": "sqlcuration-root",
  "version": "1.0.0",
  "description": "Root orchestrator for SQL Curation application",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && .\\.venv\\Scripts\\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000",
    "dev": "npx -y concurrently --kill-others --names \"backend,frontend\" -c \"yellow,blue\" \"npm run dev:backend\" \"npm run dev:frontend\""
  }
}
